/* Dado um Array de nomes, mostre na tela todos os nomes de trás para frente (o último item do Array é o primeiro da página) */

const nomes = ['Jõao', 'Maria', 'Sebastião', 'Josué', 'Amadeu', 'Joana', 'Janicleide', 'Marcos',]
let contador = 0

for(let i = nomes.length - 1 ;i >= contador; i--){
    let nomesInvertidos = nomes[i]
    console.log(nomes[i] + '  >  ' + typeof nomes[i])
    
    
    }
